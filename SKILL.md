---
name: rensheng-youji-card
description: 使用人生有迹远程MCP和一次性个人体验码，根据姓名（可选）、出生年月日时、性别和出生地点，生成含20年连续人生K线与具体当前课题的3:4免费PNG卡片。用于用户要求生成“人生有迹”卡片、查看出生配置、初始天赋、核心配置、主线任务、阶段趋势或当前课题时。
---

# 人生有迹免费卡片

## 连接

使用远程MCP：

```text
https://rensheng-youji-ap-454189475786.asia-east1.run.app/mcp/
```

使用用户单独提供的一次性体验码作为Bearer Token。保护体验码，不在回复中重复显示，不写入任何公开文件或日志。

连接后确认存在：

- `prepare_birth_card`
- `render_birth_card`

## 收集输入

一次性询问姓名（可留空）、出生年月日时、出生城市及国家或地区、性别，以及时间是否已经过真太阳时校正。

- 普通钟表时间：传入 `time_basis=local_civil`。
- 已校正真太阳时：传入 `time_basis=true_solar_adjusted`。

只有信息缺失、矛盾或服务返回边界警告时才继续追问。

## 生成卡片

1. 使用完整信息调用一次 `prepare_birth_card`。
2. 核对基本信息与服务警告。
3. 没有待补充信息时，使用完全相同的参数调用一次 `render_birth_card`。
4. 展示服务器返回的PNG，并简短说明时间口径。

服务器返回的新版卡片固定依次展示：出生配置、初始天赋、核心配置、主线任务、人生K线、当前课题。人生K线为当前年前5年、当前年和未来14年；当前年份金色高亮。不要在回复中逐年扩写或解释20个年份。

不得自行排盘、改写卡片文字、重算人生K线或在本地复刻版式。不要为了额外摘要重复调用工具。

## 一次性规则

- 准备阶段只预占体验码；成功返回PNG后才永久失效。
- 没有生成PNG的失败允许使用原码重试。
- 临时错误最多自动重试一次出图；成功后不得再次调用。
- `401` 表示无效、过期或已使用；`409` 表示并发或已核销；`503` 表示服务暂时不可用。

如果客户端不能连接远程MCP，明确说明限制并指导用户完成一次连接器设置；不要要求普通体验者安装Python或打开命令行。完整执行规则见 [`AI-START.md`](AI-START.md)。
