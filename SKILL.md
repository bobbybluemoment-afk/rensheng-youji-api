---
name: rensheng-youji-card
description: 使用人生有迹轻量REST客户端和一次性个人体验码，根据姓名（可选）、出生年月日时、性别和出生地点，生成含20年连续人生K线与具体当前课题的3:4免费PNG卡片。用于用户要求生成“人生有迹”卡片、查看出生配置、初始天赋、核心配置、主线任务、阶段趋势或当前课题时。
---

# 人生有迹免费卡片

## 执行方式

优先运行随 Skill 提供的 `scripts/generate_card.py`，直接请求人生有迹 REST 服务。不要要求普通用户安装插件、配置 MCP、安装 Python 包或打开命令行；由当前 AI 自行运行脚本。

脚本只负责传递出生资料、接收服务结果和保存 PNG。排盘、分析、人生K线与排版均在私有服务端完成。不得自行排盘、改写卡片文字、重算人生K线或在本地复刻版式。

## 收集输入

一次性询问姓名（可留空）、出生年月日时、出生城市及国家或地区、性别，以及时间是否已经过真太阳时校正。

- 普通钟表时间：`time_basis=local_civil`。
- 已校正真太阳时：`time_basis=true_solar_adjusted`。
- 时间统一为24小时格式：`YYYY-MM-DD HH:MM`。
- 性别传入 `male` 或 `female`。

只有信息缺失、矛盾或服务返回边界警告时才继续追问。

## 生成卡片

1. 确定本 Skill 的目录，运行其中的 `scripts/generate_card.py`。
2. 把姓名、出生时间、城市、国家或地区、性别、时间口径和输出路径作为参数传入。
3. 体验码仅通过脚本的隐藏输入或受保护的环境变量提供，不放进命令行参数、临时文件、日志或回复。
4. 脚本先调用准备接口；没有边界警告时自动调用出图接口，并保存服务器返回的 PNG。
5. 脚本因边界警告退出时，向用户核实后用修正后的参数重新运行；只有用户明确接受该警告时才加 `--accept-warnings`。
6. 展示生成的 PNG，并简短说明时间口径。不要逐年扩写20个年份。

示例命令只展示非秘密参数：

```bash
python3 scripts/generate_card.py \
  --birth "1999-05-27 14:08" \
  --gender female \
  --city "北京" \
  --country "中国" \
  --time-basis local_civil \
  --output "rensheng-youji-card.png"
```

脚本启动后通过隐藏输入提供个人体验码。若运行环境已经安全注入环境变量，可使用 `RSY_EXPERIENCE_CODE`；不得在回复中回显其值。

## 一次性规则

- 准备阶段只预占体验码；成功返回 PNG 后才永久失效。
- 没有生成 PNG 的失败允许使用原码重试。
- 成功后不得再次运行出图流程。
- `401` 表示无效、过期或已使用；`409` 表示并发或已核销；`422` 表示出生资料需要修正；`503` 表示服务暂时不可用。
- 网络中断发生在出图请求之后且无法确认服务器是否已返回 PNG 时，不要盲目重复调用；先说明状态不确定，再使用原码重试一次。

新版卡片固定依次展示：出生配置、初始天赋、核心配置、主线任务、人生K线、当前课题。人生K线为当前年前5年、当前年和未来14年；当前年份金色高亮。

## 兼容入口

若客户端已经连接人生有迹远程 MCP，也可以使用 `prepare_birth_card` 和 `render_birth_card`。不要为了使用 MCP 要求普通体验者额外安装或配置连接器；默认使用轻量 REST 脚本。完整执行说明见 [`AI-START.md`](AI-START.md)。
