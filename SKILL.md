---
name: rensheng-youji-free-card
description: 在用户本地，根据姓名（可选）、出生年月日时、性别和出生地点，确定性排出四柱与大运并生成含20年连续人生K线和具体当前课题的3:4人生有迹免费PNG卡片。用于用户要求生成“人生有迹”免费卡片、查看出生配置、初始天赋、核心配置、主线任务、阶段趋势或当前课题时；不需要验证码、MCP或人生有迹云端接口。
---

# 人生有迹免费卡片

## 原则

- 始终运行本 Skill 自带的确定性程序；不要让语言模型口算四柱、主观编造K线或替换卡片文字。
- 全部计算与渲染在当前用户的运行环境完成，不调用人生有迹服务器，不收集或上传出生资料。
- 不索要验证码，不安装连接器，不配置MCP。
- 结果用于文化体验与自我观察，不把阶段指数解释为吉凶、财富、健康或事件发生概率。

## 收集输入

一次询问以下资料：

- 姓名，可留空；
- 出生年月日和准确时间，24小时制；
- 出生城市、国家或地区；
- 性别，仅用于传统排运顺逆；
- 时间是普通钟表时间，还是已经校正过的真太阳时。

普通钟表时间使用 `local_civil`；已经校正的真太阳时使用 `true_solar_adjusted`。只有城市未收录时，再确定经度和IANA时区并传给程序。

## 本地执行

1. 定位本 Skill 目录。
2. 先运行 `scripts/check_env.py`。若返回缺少依赖，由当前AI运行 `scripts/setup_env.py`；不要把安装步骤转交普通用户。
3. 使用检查结果给出的 Python 运行 `scripts/generate_card.py`。
4. 保存并展示PNG；如用户需要核对，再使用 `--profile-output` 保存审计JSON。

示例：

```bash
python scripts/generate_card.py \
  --birth "1990-05-04 13:49" \
  --gender male \
  --city "北京" \
  --country "中国" \
  --time-basis local_civil \
  --output "rensheng-youji-card.png"
```

若 Skill 自带 `venv`，优先使用其中的 Python：Windows 为 `venv\\Scripts\\python.exe`，macOS/Linux 为 `venv/bin/python`。

## 输出检查

生成后确认：

- PNG尺寸是1242×1660；
- 内容依次为出生配置、初始天赋、核心配置、主线任务、人生K线、当前课题；
- 人生K线共20年：当前年前5年、当前年、未来14年；
- 当前年份用金色高亮；
- K线有纵轴、上下影线、实体和三年趋势，年度之间不画收盘连线；
- 单年影线范围不超过22点，年度中枢保持连续；
- 当前课题使用具体但保留余地的普通话，不使用“换轨、抓手、赋能、内耗”等AI黑话。

最后只需简短说明卡片已经在本地生成、采用的时间口径和城市中心真太阳时近似提醒。不要逐年扩写20个年份。

## 运行能力不足时

若当前AI明确不能安装自定义Skill、读取文件或运行Python，不要伪造卡片。请用户改用[人生有迹免费网页版](https://rensheng-youji-web.bobbybluemoment.workers.dev)。网页版同样在浏览器本地计算，不需要验证码。
