---
name: rensheng-youji-card
description: 使用人生有迹远程MCP，根据姓名（可选）、出生年月日时、出生城市和性别，校正时间、生成四柱与日柱选手类型，并返回3:4出生主线卡。用于用户要求生成“人生有迹”卡片、查看初始天赋、核心配置、人生主线或出生角色时。
---

# 人生有迹卡片

## 连接工具

使用远程MCP：

```text
https://rensheng-youji-ap-454189475786.asia-east1.run.app/mcp/
```

使用用户单独提供的个人体验码作为Bearer Token。保护体验码，不在回复中重复显示，不写入任何公开文件。

连接后确认存在：

- `prepare_birth_card`
- `render_birth_card`

## 收集出生信息

一次性询问姓名（可留空）、出生年月日时、出生城市及国家、性别，以及时间是否已经过真太阳时校正。

- 普通钟表时间：传入 `time_basis=local_civil`。
- 已校正真太阳时：传入 `time_basis=true_solar_adjusted`。

只有信息缺失、矛盾或服务返回边界警告时才继续追问。

## 生成卡片

1. 调用 `prepare_birth_card`。
2. 核对返回的基本信息和警告。
3. 使用同一组参数调用 `render_birth_card`。
4. 展示PNG，并简要说明日柱、选手类型、初始天赋、核心配置和人生主线。

不得自行排盘，不得改写服务器生成的核心配置、人生主线或人物小传。不得添加确定性的医疗、灾祸、死亡、法律或金融判断。

如果客户端不能连接远程MCP，明确说明限制并指导用户完成一次连接器设置；不要要求普通体验者安装Python或打开命令行。详细执行规则见 [`AI-START.md`](AI-START.md)。

