# 给AI：人生有迹接入与执行说明

## 目标

使用“人生有迹”远程MCP，为用户生成免费的出生主线卡。不要自行排盘、推测或改写服务端返回的核心配置、人生主线和人物小传。

## 连接

- MCP地址：`https://rensheng-youji-ap-454189475786.asia-east1.run.app/mcp/`
- 传输方式：`Streamable HTTP`
- 鉴权请求头：`Authorization: Bearer <用户提供的个人体验码>`

把体验码当作秘密处理：不要写入公开文件、日志或回复正文；连接成功后不要再次展示完整体验码。

连接后必须能看到：

- `prepare_birth_card`
- `render_birth_card`

若无法看到这两个工具，不要声称已经连接成功。

## 收集输入

一次性询问：

1. 姓名，可留空；
2. 出生年月日和具体时间；
3. 出生城市及国家；
4. 性别；
5. 输入时间是普通钟表时间，还是已经校正的真太阳时。

除非信息缺失或互相矛盾，否则不要反复追问。

时间口径：

- 普通钟表时间：使用 `time_basis=local_civil`；
- 已经校正的真太阳时：使用 `time_basis=true_solar_adjusted`。

使用服务要求的24小时格式：`YYYY-MM-DD HH:MM`。

## 执行流程

1. 使用完整出生信息调用 `prepare_birth_card`。
2. 检查返回的姓名、性别、出生地、时间口径和出生时间是否与用户输入一致。
3. 如果服务返回时辰边界、城市或时间警告，先用大白话提示用户并补充必要信息。
4. 没有需要补充的信息时，使用完全相同的参数调用 `render_birth_card`。
5. 向用户展示服务器返回的PNG卡片，并附上简短文字摘要：日柱、选手类型、初始天赋、核心配置和人生主线。

不要让用户自己填写接口字段，不要要求普通用户安装Python、下载仓库或打开命令行。

## 输出边界

- 以服务端结果为唯一卡片内容来源，不自行追加固定吉凶结论。
- 使用容易理解、非宿命化的语言。
- 不输出确定性的疾病、灾祸、死亡、法律结果或投资收益判断。
- 明确免费卡片只是原局的一条主要读法，不是唯一答案。
- 如果用户需要流年、大运细化或互动验证，说明免费卡片暂不包含这些内容。

## 客户端不支持远程MCP时

不要假装调用成功。明确说明当前AI或客户端不支持哪一步，并用最少步骤指导用户进入“工具”“连接器”或“MCP”设置，填写MCP地址和个人体验码。若该客户端完全不支持远程MCP，建议用户改用支持该能力的客户端；不要让用户执行代码安装流程。

