# 给AI：人生有迹免费卡片执行说明

## 目标

使用“人生有迹”远程MCP和用户提供的一次性体验码，生成服务器返回的3:4 PNG免费卡片。新版卡片固定包含出生配置、初始天赋、核心配置、主线任务、20年人生K线和当前课题。不得自行排盘、编写卡片内容或在本地复刻服务器版式。

## 连接

- MCP地址：`https://rensheng-youji-ap-454189475786.asia-east1.run.app/mcp/`
- 传输方式：`Streamable HTTP`
- 鉴权请求头：`Authorization: Bearer <用户提供的个人体验码>`

把体验码当作秘密处理：不要写入公开文件、日志或回复正文；连接成功后不要再次展示完整体验码。

连接后必须能看到：

- `prepare_birth_card`
- `render_birth_card`

若看不到这两个工具，不得声称连接成功。

## 一次收集输入

一次性询问：

1. 姓名，可留空；
2. 出生年月日和具体时间；
3. 出生城市及国家或地区；
4. 性别；
5. 输入时间是普通钟表时间，还是已经校正的真太阳时。

除非信息缺失、互相矛盾或服务返回边界警告，否则不要反复追问或增加草案确认轮次。

时间口径：

- 普通钟表时间：`time_basis=local_civil`；
- 已校正真太阳时：`time_basis=true_solar_adjusted`。

统一使用24小时格式：`YYYY-MM-DD HH:MM`。

## 执行流程

1. 用完整出生信息调用一次 `prepare_birth_card`。
2. 核对服务返回的姓名、性别、出生地、出生时间和警告。
3. 只有服务提示地点、时区或时辰边界问题时，才向用户补充询问。
4. 没有待补充信息时，用完全相同的出生参数调用一次 `render_birth_card`。
5. 直接展示服务器返回的PNG，并简短说明排盘采用的时间口径。

不要让用户手工填写接口字段，不要要求普通用户安装Python、下载仓库或打开命令行。不要为了写额外摘要而重复调用工具。

## 验证码规则

- `prepare_birth_card` 只预占体验码，不代表已经使用完。
- `render_birth_card` 成功返回PNG后，体验码永久失效。
- 如果服务失败且没有返回PNG，保留同一个体验码重试；不要立即要求新码。
- 网络或临时服务错误时，最多自动重试一次 `render_birth_card`，且必须使用同一组参数。
- 已成功返回PNG后，不得再次调用 `render_birth_card`。

错误处理：

- `401`：体验码无效、已过期或已使用。请用户核对体验码来源，不要在回复中回显完整码。
- `409`：体验码已被其他成功请求核销或出现并发冲突，不要反复重试。
- `503`：验证码存储或服务暂时不可用；说明本次未成功出图并建议稍后使用原码重试。

## 输出边界

- 以服务器返回的PNG为唯一卡片结果，不自行改变卡片文字、K线、颜色或版式。
- 不补充确定性的疾病、灾祸、死亡、法律结果或投资收益判断。
- 使用容易理解、非宿命化的语言，说明卡片是文化体验与自我观察工具。
- 免费卡片不等于完整付费报告；用户需要更深入分析时，只说明可进一步了解，不在免费流程中扩写。

## 客户端不支持远程MCP时

不要假装调用成功。明确说明当前客户端不支持哪一步，并用最少步骤指导用户进入“工具”“连接器”或“MCP”设置，填写MCP地址和个人体验码。若客户端完全不支持远程MCP，建议改用支持该能力的客户端；不要让普通体验者执行代码安装流程。
