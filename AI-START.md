# 给AI：人生有迹免费卡片执行说明

## 目标

使用随 Skill 提供的轻量 REST 客户端和用户的一次性体验码，生成服务器返回的3:4 PNG免费卡片。卡片固定包含出生配置、初始天赋、核心配置、主线任务、20年人生K线和当前课题。

不要自行排盘、编写卡片内容或复刻版式。不要要求普通用户安装插件、配置MCP、安装Python包或打开命令行。

## 一次收集输入

一次性询问：

1. 姓名，可留空；
2. 出生年月日和具体时间；
3. 出生城市及国家或地区；
4. 性别；
5. 输入时间是普通钟表时间，还是已经校正的真太阳时。

时间口径：

- 普通钟表时间：`time_basis=local_civil`；
- 已校正真太阳时：`time_basis=true_solar_adjusted`。

统一使用24小时格式：`YYYY-MM-DD HH:MM`。性别统一传入 `male` 或 `female`。

## 默认执行流程

1. 找到本 Skill 根目录中的 `scripts/generate_card.py`。
2. 运行脚本并传入完整出生资料和 PNG 输出路径。
3. 体验码只通过隐藏的标准输入提供。运行环境能够安全注入秘密环境变量时，也可使用 `RSY_EXPERIENCE_CODE`。
4. 不得把体验码写入命令行参数、请求JSON文件、公开文件、日志或回复正文。
5. 脚本先请求 `/generate`；没有边界警告时自动请求 `/render-card`，验证有效PNG后原子保存。
6. 脚本若以退出码 `3` 停止，表示服务返回需要核实的警告。只展示警告，不展示私有分析数据；向用户核实后，用修正参数重新运行。用户明确接受警告时才使用 `--accept-warnings`。
7. 成功后直接展示 PNG，并简短说明采用的时间口径。

非秘密参数示例：

```bash
python3 scripts/generate_card.py \
  --name "" \
  --birth "1999-05-27 14:08" \
  --gender female \
  --city "北京" \
  --country "中国" \
  --time-basis local_civil \
  --output "rensheng-youji-card.png"
```

不要让用户手工填写接口字段。除非信息缺失、相互矛盾或服务返回边界警告，否则不要增加草案确认轮次。

## 验证码规则

- 准备阶段只预占体验码，不代表已经使用完。
- `/render-card` 成功返回 PNG 后，体验码永久失效。
- 如果没有生成 PNG，保留同一个体验码重试。
- 已成功返回 PNG 后不得再次运行。

脚本错误：

- 退出码 `10`：体验码无效、过期或已使用；不要回显完整体验码。
- 退出码 `11`：体验码已被成功核销或存在并发冲突；不要反复重试。
- 退出码 `12`：出生资料未通过校验；核对脚本参数。
- 退出码 `13`：服务暂时不可用；若未生成PNG，可稍后使用原码重试。
- 退出码 `20`：网络或响应格式异常；不要声称已经成功出图。

## 输出边界

- 以服务器返回的 PNG 为唯一卡片结果。
- 不自行改变卡片文字、K线、颜色或版式。
- 不补充确定性的疾病、灾祸、死亡、法律结果或投资收益判断。
- 使用容易理解、非宿命化的语言，说明卡片是文化体验与自我观察工具。
- 免费卡片不等于完整付费报告；用户需要深入分析时，只说明可进一步了解，不在免费流程中扩写。

## 兼容远程MCP

客户端已经连接人生有迹远程MCP时，可以继续调用 `prepare_birth_card` 和 `render_birth_card`。但默认使用 REST 客户端，不要求普通用户安装插件或配置MCP。
